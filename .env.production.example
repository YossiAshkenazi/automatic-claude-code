# Production Environment Configuration
# 
# IMPORTANT: Copy this file to .env.production and fill in secure values
# NEVER commit the actual .env.production file to version control
#
# To generate secure random values, use:
# openssl rand -base64 32    (for JWT_SECRET and SESSION_SECRET)
# openssl rand -base64 16    (for database passwords)

# Database Configuration
POSTGRES_PASSWORD=CHANGE_ME_USE_SECURE_PASSWORD
DATABASE_URL=postgresql://acc_user:CHANGE_ME_USE_SECURE_PASSWORD@postgres:5432/dual_agent_monitor

# Authentication Secrets (minimum 32 characters)
JWT_SECRET=CHANGE_ME_USE_SECURE_32_CHAR_MINIMUM_SECRET
SESSION_SECRET=CHANGE_ME_USE_DIFFERENT_SECURE_32_CHAR_SECRET

# Redis Configuration
REDIS_URL=redis://redis:6379

# Application Settings
NODE_ENV=production
ACC_WORKSPACE=/workspace

# Optional: External Integrations
# SLACK_WEBHOOK_URL=
# DISCORD_WEBHOOK_URL=
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=