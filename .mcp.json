{
  "mcpServers": {
    "context7": {
      "type": "stdio",
      "description": "Context7 MCP pulls up-to-date, version-specific documentation and code examples straight from the source — and places them directly into your prompt.",
      "command": "powershell",
      "args": [
        "-Command",
        "npx -y @upstash/context7-mcp"
      ]
    },
    "playwright": {
      "type": "stdio",
      "description": "A Model Context Protocol (MCP) server that provides browser automation capabilities using Playwright. This server enables LLMs to interact with web pages through structured accessibility snapshots, bypassing the need for screenshots or visually-tuned models. use --isolated to run in a separate environment",
      "command": "powershell",
      "args": [
        "-Command",
        "npx '@playwright/mcp@latest' --isolated"
      ]
    },
    "memory": {
      "type": "stdio",
      "description": "Persistent memory and context management for Claude Code sessions. Store and recall information across conversations and projects.",
      "command": "powershell",
      "args": [
        "-Command",
        "npx -y @modelcontextprotocol/server-memory"
      ]
    },
    "archon": {
      "type": "stdio",
      "description": "Archon MCP server for project management, task tracking, and knowledge base integration.",
      "command": "powershell",
      "args": [
        "-Command",
        "npx mcp-remote http://localhost:8051/mcp --allow-http"
      ]
    }
  }
}